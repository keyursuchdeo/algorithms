package com.algo.arrays

object PrefixSuffixSearch extends App {
//  class WordFilter(_words: Array[String]) {
//
//    private def findPrefixes(word: String): Seq[String] = {
//      val chars = word.toCharArray
//      @scala.annotation.tailrec
//      def find(index: Int, prefixes: Seq[String]): Seq[String] = {
//        if(index == chars.length) {
//          prefixes
//        } else {
//          val newPrefix = prefixes.headOption.getOrElse("") + chars(index)
//          find(index + 1, newPrefix +: prefixes)
//        }
//      }
//
//      def findSuffixesForPrefixEndingAt(index: Int) = {
//        def find(currIndex: Int, suffixes: Seq[String]): Seq[String] = {
//          if(currIndex == chars.length) {
//            suffixes
//          } else {
//
//          }
//        }
//      }
//    }
//
//    private def prepMap(index: Int, map: Map[String, Int]) = {
//      if(index == _words.length) {
//        map
//      } else {
//        val word = _words(index)
//        word.stringPrefix
//        val wordChars = _words(index).toCharArray
//        wordChars.
//      }
//    }
//
//    def f(prefix: String, suffix: String): Int = {
//
//    }
//
//  }
}
