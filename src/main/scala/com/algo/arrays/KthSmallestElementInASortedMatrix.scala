package com.algo.arrays

object KthSmallestElementInASortedMatrix extends App {
//  object Solution {
//    def kthSmallest(matrix: Array[Array[Int]], k: Int): Int = {
//      val rows = matrix.length
//      val cols = matrix.headOption.map(_.length).getOrElse(0)
//      def findKthSmallest(row: Int, col: Int, currElementIndex: Int): Int = {
//        if(currElementIndex == k || (row == rows - 1 && col == cols - 1)) {
//          matrix(row)(col)
//        } else {
//          if (row == rows - 1) {
//            findKthSmallest(row, col + 1, currElementIndex + 1)
//          } else if (col == cols - 1) {
//            findKthSmallest(row + 1, col, currElementIndex + 1)
//          } else {
//
//          }
//        }
//      }
//    }
//  }
}
